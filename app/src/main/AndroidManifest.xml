<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rss.oc.www.absences20">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Autoriisations pour Beacon -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- bluetooth LE necessaire -->
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity"></activity>
        <activity android:name=".activity.LoginActivity"></activity>
        <activity android:name="activity.LoginActivity"></activity>
        <activity android:name=".activity.ChargementActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ProfesseurActivity"></activity>
        <activity android:name=".activity.EtudiantAbsencesActivity"></activity>
        <activity android:name=".activity.EtudiantProfileActivity"></activity>
        <activity android:name=".activity.BeaconViewerActivity" />
        <activity android:name=".activity.EtudiantSettingsActivity" />
        <activity android:name=".activity.ProfesseurAccueilActivity"></activity>
    </application>

</manifest>